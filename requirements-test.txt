# Test dependencies for the API + engine suite.
#
# Starlette's TestClient (used by tests/test_api.py via fastapi.testclient)
# needs an httpx-family HTTP client at test time; it is not a runtime dep, so
# it lives here rather than in requirements-api.txt.
-r requirements-api.txt
pytest>=7.4.0
httpx>=0.27.0
